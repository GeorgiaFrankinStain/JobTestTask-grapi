(+) переделать передачу параметров из консоли на передачу параметров через http запрос
    (+) сделать получение параметра
    (+) сделать передачу его в консоль, которая будет передавать на бэкэнд

(+) сделать потребление ответа внутри запроса get

( ) сделать упаковку в докеры

(+) привести в приличный вид
( ) вынети потребелние за рамки создания запроса (хотя у меня нету закрытия, так что скорее всего всего соединение обрнаруживается каждый раз, а не создается)

( ) запушить на гитхаб
( ) запушить на докерхаб
( ) добавить readme для docker
( ) перенести на typescript
( ) сделать финнализатор для закрытия канала, закрытия ресурсов
( ) сделать тесты


? размещать передачу параматра в качестве строки запроса или сегмента пути ?
    факторы
        : параметр обязательный
        : при увеличении количества параметров в систему, мы же не будем увеличивать адрес до бесконечности

? что будет возвращаться в случае неправильной передачи данных ?
    ? текст ошибки будет возвращаться ?
    ? персональный код ошибки будет возвращаться ?
    ? поле с кодом ошибки будет возвращаться каждый раз, или в случае ошибки оно будет отсутствовать ?
    



? почему сервер-клиент (M1) завершает работу, когда get запрос выполняет задачу ?
    ? где-то закрывается очередь ?
    ? сервер-клиент завершает работу при отсутствии потребления rabbitmq ответов ?
    
    ? а сервер должен продолжать работу при наличии ответа от app.get ?
    ? с чистым hello-world работает как ?
        не завершается работа сервера
